{
  "manifest_version": 3,
  "name": "Drag to summarize",
  "version": "1.0",
  "description": "A minimal extension to summarize text on a webpage using Chrome's built-in AI",
  "action": {
    "default_popup": "popup.html"
  },
  "permissions": [
    "activeTab"
  ],
  "content_scripts": [
    {
      "matches": ["<all_urls>"],
      "js": ["content.js"]
    }
  ]
}
